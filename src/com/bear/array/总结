- 原地哈希
1. 一般数组下标去映射代表值， 数组值表示具体的含义， 如是否存在等
2. 在哈希的时候一般采用加负号标记或加长度的偏移量， 都是为了能够再找到原本的值
做题步骤：
遍历元素， 首先找到目标下标地址（不管是取绝对值还是取模来恢复值），
         然后判断目标下标的值是否被标记， 有则加入目标数组， 没有则标记

- 计数数组, 数组下标表示值, 数组值代表次数, 用来解决重复,次数等问题

- 双指针, 可以解决最大连续数字,

多项式的表示：
顺序存储：
    1。 首先可以用数组来表述，数字下标代表指数，数组值代表系数， 但是这存在问题，如果指数差距特别大，就会造成很大的空间浪费。
    2。 使用二元祖来表示，其中一个表示指数，一个表示系数。
链式存储：
    1。 一个节点存放一个指数，一个系数和下一个节点
同一个问题可以有不同的表示方法（存储）