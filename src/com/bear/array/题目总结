485 最大连续1的个数
    使用双指针, 左指针指向第一个是1的位置, 右指针指向第一个不是1的位置
141 第三大的数字
    简单模拟返回即可, 注意读题
628 三个数字的最大乘积
    排序比较, 最大的乘积要么是最大的三个数字, 要么是前两个和最后一个

26, 删除重复的元素,
    使用双指针, 左指针不重复的元素,右指针遍历寻找, 遇到与左相同的元素就右移, 遇到不相同的就左指针右移, 将右指针的值赋值左指针,右指针右移继续遍历
121. 买卖股票的最佳时机
    使用一个变量一直记录最小值, 遍历, 如果遇到更小的更新最小值, 否则更新利润

122. 买卖股票的最佳时机 II
    双指针, 左指针指向局部最低值, 右指针指向局部最高值,
    贪心算法, 在每一步总是做出当前看来最好的选择,局部最优导致全局最优

189. 轮转数组
    1 根据轮转的次数可以算出来对应索引的规律, 遍历数组即可找到目标索引, 用一个临时数组存储然后拷贝
    2 数组旋转,

217. 存在重复元素
    1. 使用HashSet判断是否重复
    2. 排序, 判断相邻元素是否相同

136. 只出现一次的数字
    1. 使用异或运算符, 相同为0, 0异或任何等于任何, 即可返回想要的数据