栈中一个元素可以存放多个信息，
使用数组来实现栈：
    优点：简单，缺点，扩容比较复杂
使用链表实现栈：使用头节点指向栈顶元素比较简单。
    优点：不需要扩容，缺点，额外空间

栈和队列很相似

二进制总结
1。 n&(~n+1)可以取得最右边的一位n, ~n+1 = -n, 所以n&-n 可以取得最右边的一位1
2。 n&(n-1)清除最右边的一位1,因为最右边的一个1的右边的0会变为1，比如110100-> 110011,最后的一个100变为011